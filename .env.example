DATABASE_URL=postgres://postgres:postgres@db:5432/localevents?sslmode=disable
CLERK_SECRET_KEY=sk_test_xxxx
TICKETMASTER_API_KEY=
PORT=8080
CORS_ORIGINS=http://localhost:3000
SCRAPER_ENABLED=true
SCRAPER_CRON_SCHEDULE=0 */6 * * *
DIGEST_ENABLED=false
# Weekly daily-planner generation; runs on the digest schedule.
# Defaults to DIGEST_ENABLED when unset.
PLANNER_ENABLED=false
# Cloudflare Turnstile secret for verifying the public /api/subscribe signup.
# Empty disables verification (local dev). Frontend uses VITE_TURNSTILE_SITE_KEY.
TURNSTILE_SECRET_KEY=
